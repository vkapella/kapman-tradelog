# Open with Design

The single record of what is in flight with the design package, what came
back, and what has been found but not yet sent. **All three sessions read and
append here**; the Tradelog session (theme owner) consolidates and sends.

Why this file exists: findings were previously dripped to Design as one-off
documents from three sessions independently. Design received two partial views
of the same defect (the version-chip overflow, hit separately by Tradelog and
the Screener), one document that was stale before it was read, and two open
threads from one repo at once.

## How to use it

**Classify every finding. Only ever stall on the first kind.**

| Class | Meaning | What the finder does |
|---|---|---|
| `BLOCKING` | Cannot proceed without a ruling | Append here, tell the operator immediately, stop that issue only |
| `RULING` | Can proceed on a stated assumption | Implement, state the assumption in the commit, append here |
| `NOTE` | Spec should record it; no action needed | Append here only |

Append to **Queued**. Do not send to Design yourself — the theme owner batches,
dedupes, and merges corroborations, because two repos independently hitting one
defect is far more persuasive than one repo asserting a rule.

Batches go out when a repo finishes an issue, or when a `BLOCKING` item forces
a send anyway (blockers carry the batch with them).

## Re-vendoring: detection and authorisation are separate

The Screener built a vendor-integrity gate that notices when upstream
`design/` has moved and its copy is stale. Keep it — it is better than
anything the governance originally specified, and it belongs in all three
repos. But it answers only half the question.

**A gate can see that the file changed. It cannot see whether the owner had
finished changing it.** Freshness and completeness are different properties,
and only the owning session knows the second one. On 2026-08-30 the Screener
detected `6d78e55` and re-vendored immediately; its copy came out
byte-identical to the announced SHA and nothing needed redoing — but only
because that change happened to be atomic. Had the gate fired two commits into
a three-commit theme change, it would have vendored a half-state and been
confident, because the file was new and looked coherent.

So:

| | Job |
|---|---|
| **The integrity gate** | Detects drift and **alerts** — "upstream moved, my copy is stale". It never authorises. |
| **The Vendor SHA below** | **Authorises.** The owner names it only once integration is complete. Re-vendor when it is newer than your copy. |

Never re-vendor on drift detection alone.

---

## OPEN — sent, awaiting ruling

### `design-lockup-defects.md` — sent 2026-08-30
| Item | Class | Status |
|---|---|---|
| UI-3 acceptance line is untestable ("chip equals the latest `fly releases` value" — Fly does not expose the release version to the Machines runtime) | `RULING` | Implemented as short SHA. **Reworded in the reissued bundle** — `issues/kapman-tradelog-issues.md:226` now reads "the chip equals the short SHA of the built commit and `GET /api/health` reports the same value". Closed 2026-09-02. |
| Lockup has no width budget and no overflow rule | `NOTE` | Bounded locally; rule still wanted — hit in 2 of 3 repos |
| Decision 09's "the type monogram is the shipping mark" describes the fallback, not the shipping mark | `NOTE` | Corrected locally; wording still misleads Fair Value |

### `design-spec-additions.md` — sent 2026-08-30
| Item | Class | Status |
|---|---|---|
| 1. New rule: a primitive must own its box (theme was authored under Tailwind preflight; plain-CSS consumers inherit app padding) | `NOTE` | `.km-icon-btn` fixed (`099890c`); rest unaudited |
| 2. Nav primitives belong in Reference §04, not only in code | `NOTE` | Shipped in theme; not in the reference |
| 3. `--pos`/`--neg`/`--warn` has never been enforced anywhere — 103 stock-palette sites in the theme owner | `RULING` | Proposed as UI-D |
| 4. Should the authoring repo consume its own vendored theme? (tokens hand-synced in two files today) | `RULING` | Hand-syncing continues meanwhile |
| 5. Decision 04 needs a concurrency caveat | `NOTE` | Branch-handoff governance already in force |

---

## RULED — came back, and what changed

| Amendment | Ruling | Landed |
|---|---|---|
| 01 | Inset pattern sanctioned via `.km-inset`; `--surface-inset` withdrawn | `9980c1a`, `07b37df` |
| 01 | Decision 38 scopes decision 22 — complete the tab pattern only where a real tab widget exists; link strips take `<nav>` + `aria-current` | `c0d4f44` |
| 01 | Five unmapped roles ruled (modals, placeholders, progress tracks, calendar cells) | `07b37df` |
| 02 | UI-C enumerations ratified — 4 of 6 inferences corrected, 2 missed sets added | `8cddb2d` |
| 02 | Decision 41 — enum values are not display labels | `1a8d8c6` |
| 02 | Decisions 42/43 — Account-kind and Event chips exempt | `8cddb2d` |
| 02 | Decision 44 — Sign out moves to the drawer footer | `1d09ea2` |
| 02 | Decision 45 — calendar inset ruling withdrawn (no referent) | `07b37df` |
| 02 | Decision 28 closed at 30px; measured contrast table replaces §02 arithmetic | `afb5f17`, `036172c` |
| 03 | Decision 53 — `z-[1]`/`z-[2]` for intra-component sticky stacking is sanctioned; the `--z-*` scale does not extend downward. Seven sites suppressed with the written reason, canonical statement at `column-config.ts` | `26f4898` |
| 03 | Decision 54 — the touch floor loses its width clause: one bare `(pointer: coarse)` rule, all five controls. **Fixing it revealed the old rules had never applied to two of them** — see below | `26f4898` |
| owner, 2026-09-02 | **Ruling 57 — the grid CSS row-height query moves to `(max-width: 1023.98px)`; the pointer clause stays.** The copied file was the outlier against both the README ladder and Tradelog's `globals.css:147`; the 768–1024 rail band now takes `--row-h-touch` in every repo that vendors it. Measured old-file-first (800 and 1023 coarse: 30 → 44; 1024 and fine: 30). Fair Value's JS mirror in `KapmanGrid.jsx` must move with the re-vendor. Numbered by the owner pending Amendment 04. | `5052040` |
| owner, 2026-09-02 | **Ruling 58 — set filters take Tradelog's semantics: nothing checked on open, a click adds, commit on Apply, Escape reverts.** Closes #109(b). Fair Value (all-checked, exclude, per-toggle commit) and the Screener ("first click means only this") both change; persisted filter shapes do not. Written up in `design/README.md` § Set filters. | siblings |
| 03 | Decision 49 — `--pos-border` / `--neg-border` / `--warn-border` at 30%; the theme's 40%/45% sites converge, `.km-remove:hover`'s 8% fill becomes `--neg-dim`. Semantic colour now has `--accent`'s three levels; more emphasis steps to the solid token, never a bespoke percentage | `af0c364` |

---

## QUEUED — found, not yet sent

| Item | Class | Found by | Detail |
|---|---|---|---|
| `--pos`/`--neg` migration scope | `RULING` | Tradelog lint | **Corrected baseline after D0: 143, not the 233 first reported to Design.** palette-class 103 (verified real, in `className` contexts), token-escape 33, z-index 7. The original total was inflated by lint false positives — 63 GitHub issue references (`#340`) matched as hex, the token-definition file linted against itself, and primitive rules applied to the repo that authors rather than consumes the theme. Proposed as UI-D. **CLOSED 2026-09-02 — done under other issues, never filed as UI-D. Measured: zero `zinc-`/`emerald-`/`rose-`/`amber-`/`sky-` utility classes remain in any of the three repos' source.** |
| Lint false-positive fixes should reach Fair Value | `NOTE` | Tradelog D0, 2026-08-30 | Four fixes made to the shared lint, all reproducible in the authoring repo and two in any repo: 3-digit hex matched issue references; the app's own token file was linted for raw colour; `shadowed-primitive`/coverage assumed a consuming repo; and a suppression reason containing a semicolon silently failed, because the walk-back treated comment prose as a statement boundary. That last one is the worst — a tool that silently ignores a written exemption stops being trusted. |
| "Text on an accent fill" has no token | `NOTE` | Tradelog D0, 2026-08-30 | `.km-btn--primary` in the theme and the filter-count badge in the app both need near-black text on an accent background, and both reached for the literal `#0d0f14`. The app now uses `var(--surface)`, which is that value, but semantically it is "text on accent", not "a surface". Worth a token if a third site appears. **SUPERSEDED 2026-09-01 — the third site appeared, then two more, and the framing here is too narrow: it is not text on *accent*, it is ink on any filled chip. Escalated to the `RULING` below, with measurements.** |
| Vendor-integrity gate should be shared tooling | `NOTE` | Screener, 2026-08-30 | The Screener built a gate that detects upstream `design/` drift against its vendored copy. Worth having in all three repos, which by the same argument as the lint and the contrast gate means it should live in `kapman-tradelog/scripts/` and be copied out, not reimplemented per repo. **Delivered:** a 73-line patch adding a `vendor-integrity` rule to `scripts/check-design-system.mjs`, at `kapman-polygon-viewer/docs/handoffs/vendor-integrity.patch` (+ `.md`). Re-checked against `9e3b121`: still applies clean. Configured by `KAPMAN_VENDOR_DIR` / `KAPMAN_THEME_SOURCE`, both defaulting to `<repo>/design`, so in the authoring repo it is a self-comparison and stays silent — it does not have the authors-vs-consumes problem `3ab9cc0` had to fix elsewhere. Missing source skips loudly rather than passing. Verified in all four states. In the Screener it has already caught one real stale copy (`6d78e55`). That repo retires its own `check-theme.ts` once this lands. |
| UI-4's v2 saved-view shape breaks a cross-language contract | `BLOCKING` | Screener, 2026-08-30 | UI-4 widens `SavedView.filters` from `Record<string, string[]>` to a `set`/`number` union. In the Screener that value is not frontend-only: `frontend/src/viewFilter.ts` has a **Python twin** (`backend/app/export_pass1.py::apply_view_filters`) and the two are pinned against each other by a node/esbuild **parity harness** (`frontend/parity/harness.ts`, driven from `backend/tests/test_export_pass1.py`). Widening the type breaks the twin and the harness and changes what `/api/export/pass1` accepts — a coordinated three-surface change the issue does not mention. **Ruling wanted:** is the numeric range filter worth that, or should it be expressed without changing the persisted `filters` shape (e.g. a sibling key the twin ignores)? UI-4's theme, pinned group and filter staging shipped; only this is held. **RULED — Amendment 03 decision 48: `filters` never widens; `rangeFilters` ships as an additive sibling key the Python twin ignores, and `/api/export/pass1` refuses a view carrying one. Implemented and deployed in the Screener at `565f59c`; `viewFilter.ts`, `export_pass1.py` and the parity harness are untouched and their tests pass unchanged.** |
| UI-2 and UI-3 acceptance lines cannot both hold | `RULING` | Screener, 2026-08-30 | With `useNarrow()` at 768 (what UI-2's scope line directs), 800px measures the **desktop** toolbar beside the 56px rail. UI-2's acceptance — "at 800px the compact toolbar and the icon rail show together" — requires the query at 1024. UI-3's acceptance ("at 800px the rail is 56px and the grid gets viewport − 56px") passes as built, measured. The README ladder table puts the compact toolbar in the 768–1024 band, which argues for 1024. Built at 768 per the explicit scope line; one of the two lines needs rewording. **RULED — Amendment 03 decision 52: the query is 1024 and the scope line was the error. Implemented in the Screener at `24a6a64`, but NOT as the one-line change the scope directs — see the new entry below.** |
| `token-parity` assumes the authoring model; a consuming repo has no `:root` | `NOTE` | Screener, 2026-08-31 | New rule 6 (`3ab9cc0`) reports **48 findings** in the Screener, all false. It requires every vendored token to also exist in the app's own `:root` — correct for Tradelog, which authors `globals.css` and generates `design/` from it. The Screener **imports `design/kapman-ui.css` directly and deliberately has no `:root` of its own** (deleting it was the point of its UI-1). Satisfying the rule would mean hand-duplicating 48 token declarations — reintroducing exactly the hand-sync drift the rule exists to prevent. **RESOLVED by `2336389`, which post-dates the `3ab9cc0` this was measured against: the rule is skipped where the app imports the theme. Measured on the Screener with the paths fixed — 0 findings, down from 48. Nothing owed.** Same authors-vs-consumes split `3ab9cc0` fixed for `shadowed-primitive` and coverage: suggest gating rule 6 the same way — if the app imports the vendored theme, parity is satisfied by construction. |
| `shadowed-primitive` threshold still misfires in a consuming repo | `NOTE` | Screener, 2026-08-31 | Survived `3ab9cc0` (which fixed the authoring-repo gating, not the threshold): **33 findings** in the Screener, nearly all false. Three shared declarations is a CSS idiom, not shadowing — `.fl-head` "shadows `.km-header`" on `display:flex; align-items:center; gap`; `.docs-table th` likewise; `.pb-col` "shadows `.km-version-chip`" on mono/border/radius. It also got *worse* when `.km-version-chip` gained its 13ch ellipsis bound: a legitimate primitive change manufactured new false positives, which is a bad property for a gate. Suggest requiring at least one distinctive (token-valued colour or border) declaration rather than layout properties alone. **RESOLVED by `ffc7258`, but not as suggested. The token-valued guard (`2336389`) took 33 to 22 and kept the named false positives, because border and radius are token-valued in every rule in the system. The rule now scores COVERAGE of the theme primitive — 3 shared and 50% — giving 2 findings on the Screener's `styles.css`. That also inverts the property objected to here: unshared declarations grow the denominator, so enriching a primitive lowers scores against it, and `.pb-col` goes 27% to 29% and stays filtered. Message reworded as a lead rather than a verdict — the Screener confirmed both survivors are true findings under the wrong name. Known bias documented in the rule: coverage favours small primitives.** |
| UI-4's filter-staging rationale does not hold in the Screener | `NOTE` | Screener, 2026-08-30 | Its acceptance is "toggling five filter values produces one autosave write, not five". Here that is **zero either way**: `handleSaveLayout` deliberately does not capture grid filters, `GridHandle.getFilters()` has no callers, and the profile autosave reports only watchlist/view/options/tab. The other stated reason — re-filtering 224 symbols per toggle — is real and is what staging was implemented for. Acceptance line untestable as written. |
| Brand mark needs a ≥512px master | `NOTE` | Tradelog, 2026-08-31 | `assets/kapman-mark.png` is 256×256. It downscales beautifully to the 180px iOS home-screen icon and 192px PWA icon, but the manifest also wants **512×512**, which is a 2× upscale. Acceptable with Lanczos on flat-shaded artwork, not ideal. A 512 or 1024 master in the bundle would fix it for all three apps. Separately, the mark is unreadable at favicon sizes (32px muddy, 16px colour blobs) — inherent to two animal heads, not to the pipeline. If the browser-tab icon matters, a simplified small-size variant is the only real fix, and it should be a bundle asset rather than three local improvisations. |
| **The theme's coarse-pointer floor had never applied to `.km-icon-btn` or `.km-pin`** | `NOTE` | Tradelog, decision 54 implementation, 2026-08-31 | Found while consolidating the five rules, and it corrects this tracker's own earlier entry, which asserted those two "take 44px under bare `(pointer: coarse)` at **any** width". They took it at no width. `@media (pointer: coarse)` was authored at `kapman-ui.css:226`, *above* `.km-icon-btn` (232) and `.km-pin` (295); a media query adds no specificity, so each control's own `min-height: 36px` won on source order. Measured in headless Chromium before the fix: 36px at 800px and at 1366px under a coarse pointer, while `.km-btn` and `.km-input` — both defined *before* their rule — correctly reported 44px. **`.km-remove` is an `.km-icon-btn`, so UI-5's destructive control was one of the two.** Both siblings vendor this file and inherit the defect verbatim; it is fixed in the announced SHA. Two lessons for the reference: a rule that must beat a same-specificity declaration has a *placement* requirement, and a spec that states a touch floor should require it to be **measured**, not read off the source — the inconsistency was visible in this file for a day and the wrong half was believed. |
| Column reorder coverage | `NOTE` | Tradelog UI-2 | Reorder lives in `ConfigVirtualTable` only; positions and imports still render pre-migration header rows (#340 debt, not UI-2 scope). |
| Column width formula budgets for neither header furniture nor an in-cell chip | `RULING` | Fair Value UI-5 | `ceil(longestWord × 7.5) + 30` measures the longest **word**, which only works for a header that wraps — an ellipsizing one needs its whole label. Two things then cropped the result. **(a)** A sortable, filterable header carries a sort indicator and a filter button beside the label — 40px together as measured in the shipped theme — and the formula budgets nothing for them, so every filterable header in the migrated grids truncated. **(b)** UI-C mandates a 64px `pin`/`model` chip beside every category score, and the formula budgets nothing for cell content, so values clipped to "18…" behind their own markers. Implemented locally as a header affordance plus a content floor (value + chip + padding); wanted as one rule the three repos share, since the Screener's grid will hit both. Note the two specs cropped each other: UI-C mandated the chip and §05 sized the column, and neither knew about the other. |
| The 76px pinned symbol column cannot also host a filter | `NOTE` | Fair Value UI-5 | The pinned group is fixed at 40+44+76=160px and UI-5 asks for header filters; at 76px the label plus a filter button does not fit. Resolved locally by keeping sort on Ticker and dropping its filter — identity is what the column is for and the metric columns carry the filters — but the two rules are in direct conflict and the sibling grids will meet it. |
| A grid row has one height, and no variant for a cell carrying provenance | `RULING` | Fair Value UI-5 | `--row-h` is 30px. Fair Value's intrinsic table shows each EPS with its source and timestamp beneath the value, and the growth suggestion shows value, confidence and delta — three lines, by deliberate earlier design. At 30px the provenance is simply lost. Implemented as a multiple of `--row-h` (`rowLines={3}`) rather than a literal height so UI-7's decision stays upstream, but the spec should say whether a tall-row variant is sanctioned or whether provenance belongs somewhere other than the cell. |
| AG Grid's set filter is Enterprise | `NOTE` | Fair Value UI-5 | UI-5 asks for "set filter on Signal and judgment columns" in all three repos; `agSetColumnFilter` is an Enterprise feature. Fair Value built a Community equivalent (distinct values, toggleable, all/none) rather than take a paid licence for a checkbox list. Worth stating in §05 so neither sibling assumes a licence — and the Screener already ships AG Grid Community, so it has the same constraint. One implementation detail costs an hour if unstated: `doesFilterPass` declared on the colDef is silently ignored for a custom React filter; it has to be registered through `useGridFilter`, or the filter renders and never filters. |
| Measured ratios corroborated in a second repo | `NOTE` | Fair Value UI-8 | Fair Value's gate run reproduces Tradelog's token table **exactly** on every shared token — `--text-4` on canvas 2.79 (published 2.6), `--text-3` on `--surface-3` 4.62, `--text` on canvas 16.66, `--accent` on canvas 11.03. Amendment 02 asked that a disagreement be treated as a finding; agreement to two decimals across two independent implementations is the other half of that, and retires any doubt that §02's arithmetic was the problem rather than the tokens. 2,072 nodes, 0 failures, self-test passing. |
| `--text-4` on a disabled *label* — "never prose" vs. UI-0 step 5 | `RULING` | Fair Value UI-8 | The two rules disagree and every repo will hit it. §02 says `--text-4` is "non-text only … never prose"; UI-0 step 5 enumerates `disabled:` sites as sanctioned `--text-4`. A disabled button reading "Preview 3 tickers" is both: an inactive control (WCAG 1.4.3 excludes it, so not a conformance failure) and unmistakably prose (2.34:1 on `--surface-3` — unreadable). Exempted while disabled only, and flagged rather than settled locally. Options: (a) keep it, `--text-4` means "non-text **or** inactive"; (b) disabled labels go to `--text-3` (4.62:1 on `--surface-3`, still visibly disabled via the fill and cursor), and `--text-4` keeps its literal meaning. |
| Em dash at `--text-4` is a marker, not prose — worth stating | `NOTE` | Fair Value UI-8 | 77 of Fair Value's 78 initial gate failures were one class: the em dash meaning "no value in force", at 2.69:1. The theme already settles it (`.km-cell--missing`), but the reference does not say so where the contrast rule is stated, so each repo re-derives it under a failing gate. Worth one line in §02 next to the `--text-4` swatch: an em-dash placeholder is a sanctioned non-text site. |
| The grid CSS's row-height query contradicts the README ladder | `NOTE` | Fair Value, decision 54/55 re-vendor, 2026-08-31 | Raised while judging every `(pointer: coarse) and (max-width: …)` in the repo after decision 54, per the ruling that control floors lose the width clause and row density keeps it. `design/kapman-grid.css:35` and `:43` both scope `--row-h-touch` to `(pointer: coarse) and (max-width: 767px)`, so the **768–1024 band takes `--row-h` (30px)** — but README's ladder table (`design/README.md:190–192`) puts that band on `--row-h-touch`, and Fair Value's UI-5 acceptance says the same ("pointer height at ≥1024, 44px below"). Decision 55 says row density stays width-driven by the UI-1 ladder, which also argues the `(pointer: coarse)` half should not be there at all: an iPad in the rail band is exactly the device the ladder is describing. Fair Value's `KapmanGrid` mirrors the vendored query rather than the ladder, deliberately — the JS number that drives AG Grid's virtualisation must agree with the CSS min-height or the two fight — so **this is held rather than forked locally**. One of the two needs to move; if the query does, both siblings re-vendor. **Corroborated by the theme owner, and it is worse than stated:** `kapman-grid.css` does not just disagree with the README, it disagrees with the other half of its own theme. Tradelog's app implements the ladder version — `globals.css:147` scopes the identical row-height rule to `(pointer: coarse) and (max-width: 1023px)` — so a tablet in the 768–1024 band gets 44px rows in Tradelog and 30px rows in any repo driven by the vendored grid CSS, from one design system. That makes `:35`/`:43` the outlier rather than a two-way spec disagreement, and points the fix at the query. Held for a ruling rather than fixed here: it forces a re-vendor, and decision 55's "width-driven" wording should be confirmed to mean the pointer clause comes off entirely. **RULED — owner ruling 57, `5052040`: the query moves to 1023.98px; the pointer clause stays, and the decision-55 reading remains the one open question.** |
| `.km-grid-col-symbol`'s fixed 76px overflows its own 76px cell | `NOTE` | Fair Value, decision 50, 2026-08-31 | `design/kapman-grid.css:76` sets `width: var(--pinned-symbol)` — 76px, the *column's* width — on the span **inside** the cell. With `--ag-cell-horizontal-padding: 8px` (`:32`) the cell's content box is 74px at best, so the span is wider than the box that holds it by construction. Measured at 1440×900: the cell's `scrollWidth` exceeded `clientWidth` by 14–16px on every row. It stays invisible only while the span is `display: inline`, where `width` does not apply — the moment a consumer makes it a block or a flex item to satisfy decision 50's yield order (cell text truncates first), it overflows the frozen boundary. Fair Value overrode it with `w-full` / `flex-1 min-w-0`. Suggest the theme rule use `max-width: 100%` (or `width: 100%`) rather than the column width, since it is sizing a child of a cell the grid has already sized. |
| **UI-2's one-line scope collides with UI-3 wherever the breakpoint hook is shared** | `RULING` | Screener, 2026-08-31 | Decision 52 rules the query, and the ruling is right; its *scope line* ("change `useNarrow()`'s media query … in both the initial `useState` and the `addEventListener` subscription") assumes the hook has one consumer. In the Screener it has two: UI-3 made the same hook the nav's source, and `App.tsx:568` drives the workspace tablist's `aria-orientation` off it — the hook's own docstring said "one matchMedia, one breakpoint". Applied literally, moving that query to 1023.98 would report `aria-orientation="horizontal"` for a **vertical** 56px rail across the whole 768–1023 band: a layout contradiction traded for an a11y one. **The ladder has two rungs with different consumers** — the toolbar flips at 1024, the nav's axis only at 768, because sidebar and rail are both vertical. Implemented as a split (`useNarrow()` 1023.98, `usePhone()` 767.98) over one `useMediaQuery` helper. This also uncovered a live one-pixel instance of the same defect: at exactly 768px the old `(max-width: 768px)` matched while the vendored nav's `(max-width: 767.98px)` did not, so the tablist already misreported its axis at one width. Worth stating in the reference that the ladder's rungs are per-concern, and that a shared breakpoint hook is not a single value. |
| Coarse-pointer floor: corroborated by measurement in the second repo, and a third control was missing it | `NOTE` | Screener, 2026-08-31 | Independent confirmation of the entry above (Tradelog, decision 54), which predicted "both siblings vendor this file and inherit the defect verbatim". Measured in the Screener's vendored copy before re-vendoring — headless Chromium, `hasTouch`+`isMobile`, `getComputedStyle().minHeight` at 1366×1024 and 800×1000: `.km-icon-btn` 36, `.km-remove` 36, `.km-pin` 36 under a coarse pointer at both widths, exactly as predicted. **`.km-nav-item` measured 38px at 1366**, missing the floor by a *different* mechanism — not the cascade, but its own `(pointer: coarse) and (max-width: 1023.98px)` width clause, which denied the 12.9″ iPad in landscape that decision 54 names. So three controls plus the rail item, not two. After re-vendoring at `26f4898`: all seven nodes 44px under a coarse pointer at both widths, 36px under a fine pointer (`.km-nav-item` 38). Per the zero-nodes lesson the measurement was run against the OLD file first, so it is known to fail when it should. The Screener has **no app-side `.touch-target`** to migrate; its only `(pointer: coarse) and (max-width:` rules are the two row-height ones in the vendored grid CSS, which keep the width clause per decision 55. |
| The shared lint's source rules silently switch themselves off in a consuming repo | `NOTE` | Screener, 2026-08-31 | Distinct from the two `token-parity` / `shadowed-primitive` entries above, which are about a rule's *semantics* in a consumer; this is about whether they run at all. `SRC` (`check-design-system.mjs:26`) and `APP_CSS` (:43) derive from **the script's own location**, not cwd — only `KAPMAN_VENDOR_DIR` / `KAPMAN_THEME_SOURCE` are env-configurable (:36–41). `CONSUMES_THEME` (:159) is then computed from that `APP_CSS` + `walk(SRC)`. Running the invocation documented for consumers (`kapman-polygon-viewer/docs/handoffs/vendor-integrity.md`) from the Screener's root against the sibling's script path therefore evaluates the flag over **Tradelog's** source, which does not import `kapman-ui.css` because Tradelog authors it → `CONSUMES_THEME` false → rule 6 (:207) and `shadowed-primitive` (:253) are **gated off entirely**, and the run prints `Primitive coverage: n/a — this repo authors the theme` followed by `No design-system findings`. That is the silent-success failure mode this programme has now hit four times: a clean report for a repo the tool never read. `vendor-integrity` is unaffected and correct — verified by planting a one-line drift in the vendored copy (it fails, naming the file and the diff command) and removing it (silent). Copying the script outward does not fix it either: the Screener's source is `frontend/src` and it has no `src/app/globals.css`, both hardcoded. **Wanted:** `KAPMAN_APP_SRC` / `KAPMAN_APP_CSS` alongside the two existing vars, and a loud skip when the app source is not found — the same pattern `vendor-integrity` already uses for a missing theme source. Until then the Screener cannot adopt the shared lint as planned and keeps `check-theme.ts`. **ACTIONED — `ffc7258`. `KAPMAN_APP_SRC` / `KAPMAN_APP_CSS` added; `THEME` now derives from `VENDOR_DIR` so a consumer lints the copy it ships; missing app source hard-fails when the path was explicitly configured and warns-and-skips on defaults. One correction to the report: `token-parity` was NOT gated off — its branch is inverted, so a false `CONSUMES_THEME` makes it RUN over the authoring repo's files. Proved by planting a token drift in `globals.css` and re-running the consumer invocation: it fired naming `src/app/globals.css`, a path the Screener does not have. The headline stands unchanged. Also closed the trap rather than only making it fixable — a foreign `KAPMAN_VENDOR_DIR` with the app source defaulted now warns, and every run prints the source it actually linted, so a clean summary can no longer describe a repo nobody asked about.** **Also worth the owner's eye:** the 48 `token-parity` and 33 `shadowed-primitive` counts attributed to the Screener above cannot have come from this invocation. Not asserting how they were obtained — flagging that they should be re-derived before anyone acts on them. **RE-DERIVED, both sound: 48 is exactly the theme's distinct token count at `6d78e55`, measured with the script's own parser — every token reported against an absent `:root`. Decision 49's three border tokens took it to 51, which is why it does not reproduce today. 33 is recorded in the script's own source comment as the pre-fix count. Neither should be discarded.** |
| Headless viewport emulation does not dispatch `matchMedia` change events | `NOTE` | Screener, 2026-08-31 | A verification caveat all three repos will hit now that the ladder is the spec. Under CDP viewport emulation (Playwright/Chrome DevTools `Emulation.setDeviceMetricsOverride`), **CSS media queries re-evaluate but `matchMedia` `change` listeners never fire**: measured `mq.matches` reading correctly as `false` at 1100px while the registered listener logged nothing and the React state stayed stale. So a resize-driven breakpoint change cannot be verified by resizing — the CSS half updates and the JS half does not, which looks exactly like a broken hook. Each rung has to be measured on a **fresh load** at that width. Cost an initial false positive here before it was isolated. |
| **"Ink on a filled chip" has no token, and the rule of three has fired — five sites, two hues** | `RULING` | Tradelog + Screener + Fair Value, 2026-09-01 | Escalated from the `NOTE` above, which framed it as "text on an accent fill". That framing is wrong: the Screener's `.fl-hit-yes` puts the same ink over `--pos`, so what the theme lacks is ink on ANY filled chip. **Five sites, and the theme is one of them:** `kapman-ui.css:209` (`.km-btn--primary`, raw `#0d0f14`); Tradelog `DataTableHeader.tsx:102` (the filter-count badge, `style={{ color: "var(--surface)" }}` — the right value under the wrong name, since it is ink, not a surface); the Screener's `.fb-badge` over `--accent` and `.fl-hit-yes` over `--pos`, both raw and both currently suppressed with `design-lint-allow` pending this ruling; and Fair Value's filter-count badge. **Measured — pixel readback through Chromium, because `getComputedStyle` returns `oklch()` verbatim and reading those numbers as RGB gives nonsense (it did on the first attempt):** `#0d0f14` scores 10.61:1 on `--accent` (`#34d3e8`), 8.20:1 on `--pos` (`#54bf5c`), 5.39:1 on `--neg` (`#f14d4c`), 9.39:1 on `--warn` (`#eba941`), 8.59:1 on `--gold` (`#d1a84b`). White fails all five, 1.81:1 to 3.56:1. **So this is ONE token, not per-hue variants** — near-black is correct over every semantic fill, which is the finding that settles the naming. Thinnest margin is `--neg` at 5.39:1; record that as the constraint if that hue is ever retuned. **Proposed name `--text-on-fill`**, sitting in the existing `--text*` family, which the Tailwind alias set already exposes; `--ink-on-fill` reads better alone but invents a family for one member. **Two implementation consequences to decide with it:** (1) the token must land in `src/app/globals.css` as well as `design/kapman-ui.css`, or `token-parity` turns this repo's own gate red; (2) per decision 04 item 4 it needs a `tailwind.config.ts` alias, or Tradelog's badge stays on an inline `style` even after the token exists. **Blast radius:** a theme change, so a new Vendor SHA and both siblings re-vendor; five call sites convert and the Screener's two suppressions come out. Not built — queued for the next design session at the owner's direction. |
| Menu elevation has no token — but the rule of three has NOT fired | `NOTE` | Screener, 2026-09-01 | Raised alongside the ink token and deliberately NOT bundled with it. The Screener carries `0 8px 24px rgba(0, 0, 0, 0.45)` twice in `styles.css`, and observes that the theme documents `--surface-2` as the menus/popovers surface while shipping no elevation treatment, so every consumer improvises one. The argument from completeness is real. The argument from count is not: checked across all three repos, that is **two sites in one repo**, and Tradelog and Fair Value have no non-inset `box-shadow` at all. Two sites in one repo is that repo's convention, not a system need — the same rule-of-three test that says the ink token has earned its place says this one has not, and applying the test only when it gives the answer we want is how a design system accumulates tokens nobody asked for. **Trigger to record: a second repo needing a menu shadow makes this a token.** Until then a cast shadow wants a real black, so `--surface` is not the substitute and the local literal is correct. |
| **An unstable callback prop in a `columnDefs` memo dep list makes every grid event rebuild all columns** | `RULING` | Screener #108, 2026-08-31 | A live production bug, and a generic React + AG Grid failure both siblings can have. **Symptom:** after filtering one column, the filter button on every OTHER column stops responding — reported as "the controls are not reactive". **Cause, and it is not in the filter code:** the grid memoized every column definition on its `onRemove` prop, which the shell passes inline (`onRemove={(s) => removeSymbols([s])}`), so it is a new function each render — and the shell re-renders on every grid event, because `onFilterChanged` feeds its state. Each filter change therefore handed AG Grid a brand-new `columnDefs` array mid-interaction and the next click was consumed instead of opening the panel. **Measured against a real filter change:** `sameColDefsArray false`, `sameColDefObject false`, but `sameColumnObject true` and `sameFilterInstance true` — AG Grid does NOT destroy the columns or filter instances, it reconciles them; the click is eaten by the definition reprocessing. **Not filter-specific:** ANY shell state change rebuilds every column def, including row selection and a 60s status poll, so a panel open when the poll fires can break with no user action — which is why it presents as intermittent. **Fix:** hold the callback in a ref inside the grid and drop it from the memo deps (a cell renderer must never be a memo dependency of every column); stabilise it in the shell too. **A/B, popup count verified 0 before each attempt** so a normal dismiss-then-open cannot be mistaken for the bug — that confound caused two misreadings before the protocol was tightened, and is the measurement warning worth carrying: before 1/5 columns opened on the first click (only the one whose filter instance already existed), after 5/5. Pinned by a test asserting `columnDefs` IDENTITY across a re-render with a new callback — deep equality passes on the broken build, since the rebuilt defs are equal in value and new in identity, and identity is what AG Grid reprocesses on. **Sibling status, checked rather than assumed:** Fair Value HAS the pattern — `updateStock`/`removeStock` are plain functions in the component body (`src/App.jsx:137,181`), recreated every render, and both sit in `columnDefs` dep lists (`ScoreCardTable.jsx:127`, `IntrinsicTable.jsx`); whether it bites depends on whether its shell re-renders on grid events, which is worth one measurement there. Tradelog is NOT affected in this form — it ships no AG Grid (`VirtualGridTable`/`ConfigVirtualTable` are its own), so there is no popup to eat the click, though the underlying rule — an unstable callback in a memo dep list rebuilding a table's columns on every event — still applies to its own table. It produces no console error and is invisible in review. Same family as Fair Value's `useGridFilter` finding above: custom React filters in AG Grid fail silently, never loudly. **CLOSED for Fair Value 2026-09-01 — LATENT, not live, and the finding was right about the pattern. `updateStock`/`removeStock` are body functions in the `columnDefs` deps at `ScoreCardTable.jsx:229` and in `IntrinsicTable.jsx` (`AllocationTable` passes `[]` and is fine), but the TRIGGER is absent: nothing wires a grid event to React state. `KapmanGrid.jsx` passes only `onGridReady` and `onFirstDataRendered`, its single `setState` is a resize observer, and there is no `onFilterChanged`/`onSortChanged`/`onStateUpdated` anywhere in the repo — verified independently here. So the shell never re-renders on a grid event and the memo cannot rebuild mid-interaction. Deliberately left unfixed and untested: a test pinning `columnDefs` identity would pin behaviour that cannot currently fail and would pass on both builds. It goes live the moment anyone wires a grid event to parent state, which is the note to carry against that repo.** |
| **A filter applied before the table sees the data is invisible to the table's own filter UI — and the three set filters disagree on what a click means** | `RULING` | Screener #109, 2026-08-31 | Two parts, both from one production report. **(a) The invisible-cohort problem, which any app with preset views will hit.** The Screener's saved views filter at the DATA layer, before the grid receives a row, so they produce no entry in the table's filter model — and a chip bar built from that model showed one chip while three others silently cut the watchlist from 224 rows to 75. The count read `36 of 75 rows`, where 75 was itself a filtered number the UI never disclosed, and `Clear all` returned the user to 75 rather than 224 with nothing on screen explaining why. Reported as "none of the other filters are active". Fixed by DISPLAY only — the data-layer application is load-bearing (an AG Grid filter from the previous preset goes stale on a column-changing view switch and blanks the grid) — as locked chips under a `View` label (dashed border, lock glyph, no remove affordance, member list on hover), a count reading `36 of 75 shown · 224 in watchlist` that appears only when the view actually narrowed, and `Clear all` renamed `Clear my filters`. **The general rule worth stating in the reference: if a view or preset filters rows before the table, the table's own filter affordances lie unless that filtering is shown; and an `n of m` count must say what `m` is.** **(b) The three apps' set filters disagree on the meaning of a click — checked, not assumed.** Tradelog `ColumnFilterPanel` opens with NOTHING checked (`ConfigVirtualTable.tsx:141` passes `table.filters[columnId] ?? []`), so clicking a value SELECTS it, and it commits on Apply. Fair Value `SetFilter.jsx:61` opens ALL-checked (its `checked` prop is true when the model is null), so clicking the value you want EXCLUDES it, and it commits on every toggle with no staging. The Screener opened all-checked like Fair Value, which meant the route to "only ELIGIBLE" was press None then check one box — that confused a real user, and it is now changed so the first click on a fresh panel means "only this", with a hint stating the rule and `All`-then-uncheck as the explicit exclude gesture. **That is three behaviours for one control across a shared UI, and the Screener has now deliberately departed from the Excel/AG-Grid convention rather than from its siblings — a local call that wants a system ruling.** Worth deciding which is canonical: nothing-checked (literal, matches Tradelog) or all-checked (shows what currently passes). Whichever wins, the two apps that lose need the change, and Fair Value separately commits on toggle where the other two stage. **RULED — owner ruling 58: Tradelog's semantics are canonical (nothing checked, click adds, commit on Apply). Fair Value and the Screener change; (a) is theirs and shipped.** |

---

## Vendor SHA

Siblings re-vendor `design/kapman-ui.css` and `design/kapman-grid.css` at the
SHA named here. Do not re-vendor on your own initiative — the theme owner
announces it.

**Current: `5052040`** — owner ruling 57: `kapman-grid.css` touch rows through
the rail band. **`kapman-grid.css` only** — `kapman-ui.css` is unchanged since
`26f4898`, so a repo already there needs the grid file alone. **Fair Value:**
also move the JS mirror in `KapmanGrid.jsx` (`useTokenRowHeight`, two
`matchMedia` strings) from `767px` to `1023.98px`, or the grid engine and the
CSS disagree in the rail band. `scripts/check-design-system.mjs` is at
`62f88ef` (three fixes since `2336389`); a copied lint older than that reports
clean for rules it does not have.
Previous: `26f4898` (Amendment 03, `kapman-ui.css`); `2336389` before that.

**Decision 49** — `--pos-border` / `--neg-border` / `--warn-border` at 30%, and
the six divergent theme sites converged onto them.

**Decision 54** — one bare `(pointer: coarse)` touch floor covering all five
controls, replacing three rules. **Read this one before you diff:** it also
fixes a defect your vendored copy has been carrying. The old rule was authored
*above* `.km-icon-btn` and `.km-pin`, and a media query adds no specificity, so
their own `min-height: 36px` won on source order and the 44px floor never
applied to either — `.km-remove` included. Measure your own controls under a
coarse pointer rather than reading the source; that is how it was found.

Two consequences for a consuming repo, neither of them obvious from the diff:

- **Placement is load-bearing.** The consolidated rule must stay *after* all
  five control definitions. If you reorder or re-indent the theme file, or your
  bundler concatenates it ahead of your own control CSS, the floor silently
  stops applying again — same-specificity, source-order loss, no warning.
- **The ruling reaches your own touch-target utility.** Decision 54 names the
  five theme primitives because that is all Design knew about. Tradelog also had
  an app-side `.touch-target` at `(pointer: coarse) and (max-width: 1023px)`;
  the width clause came off it too, or an iPad in landscape would show a 44px
  `.km-btn` beside a 36px `.touch-target` button. Grep for
  `(pointer: coarse) and (max-width:` and judge each hit — **control floors lose
  the width clause; row density keeps it** (that is the UI-1 ladder, decision
  55, and is deliberately width-driven).

Semantic colour now has `--accent`'s three levels — dim (12%) → border (30%) →
solid. When you migrate your own `/40`-`/70` improvisations, a site needing more
emphasis than the border steps to the **solid** token, never to a bespoke
percentage.

UI-D0/D1/D2 remain app-side and carry nothing outward — the lint fixes, the
Tailwind aliases and the semantic migration are all local to this repo.
